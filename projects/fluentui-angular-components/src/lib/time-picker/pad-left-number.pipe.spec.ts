import { PadLeftNumberPipe } from './pad-left-number.pipe';

describe('PadLeftNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PadLeftNumberPipe();
    expect(pipe).toBeTruthy();
  });
});

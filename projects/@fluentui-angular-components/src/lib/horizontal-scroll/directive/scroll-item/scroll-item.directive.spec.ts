import { ScrollItemDirective } from './scroll-item.directive';

describe('ScrollItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollItemDirective(null as any);
    expect(directive).toBeTruthy();
  });
});

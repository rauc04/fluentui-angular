import { ScrollItemElementRefDirective } from './scroll-item-element-ref.directive';

describe('ScrollItemElementRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollItemElementRefDirective();
    expect(directive).toBeTruthy();
  });
});
